import ProviderBase from "../provider-base.js"

export default class Provider extends ProviderBase {
    
    constructor() { 
        
        super( "gapi", "Your Google identity (e.g. gmail)" );
        
    }
    
};